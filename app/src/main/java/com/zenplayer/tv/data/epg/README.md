# EPG pipeline

ZenPlayer normalizes XMLTV programme entries into `EpgProgramme`. The repository facade exposes time-window queries, while channel matching supports provider `tvg-id` and normalized names.
